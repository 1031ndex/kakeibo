# v2 への差し替え手順

作り直しにあたって、テーブル構成とアプリの画面が変わりました。
**ログインアカウントは消えません。**手順3のやり直しは不要です。

## 1. Supabase（3つのSQLを順に実行）

SQL Editor で上から順に実行してください。

1. `sql/v2_00_reset.sql` — v1のテーブルを削除する
2. `sql/v2_01_schema.sql` — 新しいテーブルを作る
3. `sql/v2_02_seed.sql` — 最終版のスプレッドシートに合わせたマスタを入れる

最後に数字（計上件数）が返れば成功です。

## 2. GitHub（ファイルを差し替える）

**削除するファイル**

- `components/Home.tsx`
- `components/Ledger.tsx`

**追加するファイル**

- `components/Calendar.tsx`
- `components/Savings.tsx`
- `components/Settings.tsx`
- `components/TxSheet.tsx`

**中身を差し替えるファイル**

- `app/page.tsx`
- `app/globals.css`
- `lib/db.ts`
- `components/Inbox.tsx`
- `gas/resona_to_supabase.gs`

zipを解凍して丸ごと上げ直す方が早いかもしれません。その場合、GitHubの
「Add file」→「Upload files」で同名ファイルを上げると上書きされます。
ただし Home.tsx と Ledger.tsx は上書きされず残るので、**個別に削除**してください。
残っているとビルドが失敗します。

## 3. GAS（貼り替える）

`gas/resona_to_supabase.gs` の中身を差し替えてください。
テーブルの列名が変わったため、旧版のままでは登録に失敗します。

スクリプトプロパティ（SUPABASE_URL など）はそのままで大丈夫です。

---

## 変わったこと

### 口座を3つに分けた

| 口座 | 中身 |
|---|---|
| 共通りそな | 2人が入れた249,000から家賃と生活費を払う |
| 貯蓄口座 | 毎月10万＋ボーナス＋還付金が入り、年間支出を出す |
| 運用資産 | 個人年金・自社株・投資信託。現金ではないので残高を混ぜない |

個人口座から出るもの（スマホ・美容室・お小遣い・奨学金・生命保険）は
アプリの管理対象から外しました。

### 画面

- **カレンダー** — 共通りそな。日付をタップするとその日の明細が出て、手入力もできる。繰越あり
- **未分類** — デビットの明細を振り分ける
- **貯蓄** — 貯蓄口座と運用資産をタブで切り替え。ボーナスと年間支出は手入力
- **設定** — 予算の変更、固定収支の確認、店名ルールの削除

### 直したバグ

- 月を戻すと貯蓄の進捗が古いまま残る問題（月の切り替えで取り直すようにした）
- 予算が「今日」基準で判定され、10月の予算が0と出る問題

### 金額をスプレッドシート最終版に合わせた

| | 旧 | 新 |
|---|---|---|
| 娯楽・買い物 | 10,000 | 19,000 |
| 拠出（りほ） | 110,500 | 115,000 |
| 拠出（ゆうき） | 129,500 | 134,000 |
| 光熱費 | 固定計上 | デビット引落（変動費） |

貯蓄口座への積立は、りほ77,000／ゆうき23,000で毎月25日に自動計上されます。
