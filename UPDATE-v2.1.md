# v2.1 の追加分

## 1. Supabase

SQL Editor で `sql/v2_03_extras.sql` を実行してください。1つだけです。

取込制御の設定、精算の計算、月次推移の集計が追加されます。

## 2. GitHub

zipの中身をアップロードして Commit changes。削除するファイルはありません。

**新しく増えるもの**

- `components/Chart.tsx`
- `components/Settle.tsx`

**中身が変わるもの**

- `app/page.tsx`
- `app/globals.css`
- `lib/db.ts`
- `components/Calendar.tsx`
- `components/Settings.tsx`
- `gas/resona_to_supabase.gs`

## 3. GAS

`gas/resona_to_supabase.gs` を貼り直してください。取込制御を読むようになっています。

---

## 追加された機能

### グラフタブ

- カテゴリ別のドーナツグラフ。**月／年**を切り替えられる
- 下に金額と割合の一覧
- 直近12か月の推移（青が入金、赤が支払）

年グラフは1年分揃うと意味が出ます。2026年は3か月分なので参考程度です。

### 精算

カレンダー画面の下に出ます。

- その月に共通りそなへ入れる額を、りほ・ゆうきごとに表示
- 立替があった月は「基準 − 立替」で自動計算
- 立替の明細も一覧で確認できる

手入力で支払者を「りほが立替」にすると、その分がここに反映されます。

### 固定収支の編集

設定 → 固定収支 → 項目をタップ。

- 名前・金額・計上日を変更できる
- 計上日は**末日**も選べる。31を指定すると、2月なら28日、4月なら30日に自動調整される
- 「今月の記帳も作り直す」にチェックを入れると、その月の記帳が新しい設定で作り直される

家賃の引落日が決まったらここで変えてください。

### 取込のオン・オフ

設定 → 取込。

- **取込を有効にする** — オフにすると新しい明細が入らなくなる
- **この日以降のメールを取り込む** — 初期値は2026-10-01

**この2つがあるので、今すぐGASをセットアップして大丈夫です。**9月以前のメールは無視され、10月1日以降のものだけが入ります。

一時的に止めたくなったら、iPhoneからトグルを切るだけで済みます。GASの画面を開く必要はありません。
